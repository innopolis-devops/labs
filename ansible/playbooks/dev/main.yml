- name: Initialize docker
  hosts: deploy_servers
  roles:
    - geerlingguy.docker
  become: yes
  vars: 
    docker_apt_ignore_key_error: true
  tags:
    - server_preparation
    - webapp_deploy

- name: deploy webapp
  hosts: deploy_servers
  roles: 
    - deployment
  tags:
    - webapp_deploy