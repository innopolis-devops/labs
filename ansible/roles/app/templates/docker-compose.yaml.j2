version: "3.9"

services:
  {{ app_name }}:
    image: {{ app_image.registry }}/{{ app_image.name }}:{{ app_image.tag }}
    container_name: {{ app_name }}
    ports:
      - "{{ app_port.external }}:{{ app_port.internal }}"
    restart: always
