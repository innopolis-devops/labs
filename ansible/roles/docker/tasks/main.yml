---
- name: Install Docker
  apt:
    name: docker.io
    state: present
  become: true

- name: Ensure Docker service is started
  systemd:
    name: docker
    state: started
    enabled: true
  become: true