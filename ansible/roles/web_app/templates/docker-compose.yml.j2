version: "3.9"

services:
  {{ web_app_name }}:
    image: {{ web_app_image.registry }}/{{ web_app_image.name }}:{{ web_app_image.tag }}
    container_name: {{ web_app_name }}
    ports:
      - "{{ web_app_port.external }}:{{ web_app_port.internal }}"
    restart: always
