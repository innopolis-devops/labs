version: "3.9"

services:
    {{ web_app_name }}:
        image: "{{ web_app_image_name }}:{{ web_app_image_tag }}"
        container_name: {{ web_app_name }}
        ports:
            - "{{ web_app.config.external_port }}:{{ web_app.config.internal_port }}"
        restart: always
