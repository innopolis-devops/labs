version: "3.9"
services:
  {{ web_app.name }}:
    image: {{ web_app.registry }}/{{ web_app.image_name }}:{{ web_app.tag }}
    container_name: {{ web_app.name }}
    ports:
      - {{ web_app.internal_port }}:{{ web_app.external_port }}
    restart: always
