version: "3.9"
services:
  web:
    build: 
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      - LOG_FILE=/var/log/visits/visits.log
    volumes:
      - data:/var/log/visits/
volumes:
  data:
