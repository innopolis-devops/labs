{
  "name": "app_purescript",
  "private": true,
  "devDependencies": {
    "buffer": "^6.0.3",
    "esbuild": "^0.14.43",
    "gh-pages": "3.2.3",
    "http-server": "^14.1.1",
    "npx": "^10.2.2",
    "parcel": "2.5.0",
    "process": "^0.11.10",
    "util": "^0.12.4"
  },
  "scripts": {
    "build": "spago build",
    "predev": "npm run build",
    "dev": "npx parcel serve -p 8001 --host 0.0.0.0 dev/index.html",
    "test": "spago test",
    "prebuild:min": "rm -rf min && mkdir min && cp dev/* min/",
    "build:min": "spago bundle-app --to min/index.js --minify && parcel build min/index.html",
    "build:gh-pages": "npm run build:min && cp -TR min docs",
    "build:prod": "npm run build:min && cp -TR min prod",
    "postbuild:prod": "npx http-server prod -o"
  },
  "dependencies": {
    "@popperjs/core": "^2.11.5",
    "bootstrap": "^5.1.3"
  }
}
