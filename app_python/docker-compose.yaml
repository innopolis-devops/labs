version: "3.9"
services:
  web:
    build: .
    ports:
      - "80:8000"
    command: ["--host", "0.0.0.0", "--port", "8000"]
    volumes:
      - moscower-storage:/storage

volumes:
  moscower-storage:
