services:
  web:
    build:
      context: ./
    env_file:
      - .env
    environment:
      - PORT=${DOCKER_PORT}
      - HOST=${HOST}
    ports:
      - ${HOST_PORT}:${DOCKER_PORT}
    volumes:
      - .:/app
