version: '3.3'

services:

  moscow_time:
    container_name: moscow-time-web-app
    image: zyymee/moscow-time-web-app:latest
    ports: 
      - "5000:5000"
    volumes:
      - "./data/visits.txt:/visits.txt"
    restart: always