version: '3.7'

services:
  web:
    image: igooorbb/inno-devops-webapp
    command: uwsgi --ini uwsgi.ini
    expose:
      - 5000
  nginx:
    image: igooorbb/inno-devops-nginx
    ports:
      - 1337:80
    depends_on:
      - web
