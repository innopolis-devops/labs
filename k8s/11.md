1.

```bash
$ kubectl create secret generic db-user-pass \
>     --from-literal=username=dariya \
>     --from-literal=password='Dariysha!'
secret/db-user-pass created


$ kubectl get secrets
NAME                                  TYPE                 DATA   AGE
db-user-pass                          Opaque               2      22s
sh.helm.release.v1.my-app-python.v1   helm.sh/release.v1   1      7d


$ kubectl describe secret db-user-pass
Name:         db-user-pass
Namespace:    default
Labels:       <none>
Annotations:  <none>

Type:  Opaque

Data
====
password:  9 bytes
username:  6 bytes


$ kubectl get secret db-user-pass -o jsonpath='{.data}'
{"password":"RGFyaXlzaGEh","username":"ZGFyaXlh"}


$ echo 'RGFyaXlzaGEh' | base64 --decode
Dariysha!
```

```bash
$ gpg --gen-key

$ sops -p <my_fingerprint> secrets.yaml

$ helm secrets install my-app-python ./my-app-python/ -f ./secrets.yaml

$ kubectl get pods

NAME                             READY   STATUS    RESTARTS      AGE
my-app-python-845d8dd596-tqkmq   1/1     Running   0             7m21s

$ kubectl exec my-app-python-845d8dd596-tqkmq -- printenv | grep MY_PASSWORD
MY_PASSWORD=bobra
```

```bash
$ kubectl describe pod app-python | grep -A 5 Limits
    Limits:
      cpu:     2
      memory:  256Mi
    Requests:
      cpu:        1
      memory:     128Mi
```