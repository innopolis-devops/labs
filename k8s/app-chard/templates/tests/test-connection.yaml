apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "app-chard.fullname" . }}-test-connection"
  labels:
    {{- include "app-chard.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "app-chard.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
