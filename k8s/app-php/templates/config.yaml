apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "app-php.fullname" . }}-config
  labels:
    {{- include "app-php.labels" . | nindent 4 }}
data:
  config.json: |-
    {{ .Files.Get "files/config.json" }}
