apiVersion: v1
kind: Service
metadata:
  name: {{ include "app_helm.fullname" . }}
  labels:
    {{- include "app_helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    {{- include "app_helm.selectorLabels" . | nindent 4 }}
