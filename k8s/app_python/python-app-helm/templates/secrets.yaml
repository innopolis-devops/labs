apiVersion: v1
kind: Secret
metadata:
  name: {{ include "python-app-helm.fullname" . }}-cred
  labels:
    {{- include "python-app-helm.labels" . | nindent 4 }}
type: Opaque
data:
  password: {{ toJson .Values.password | b64enc | quote }}