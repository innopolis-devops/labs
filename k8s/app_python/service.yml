apiVersion: v1
kind: Service
metadata:
  name: python-application-service
spec: 
  type: LoadBalancer
  selector:
    app: python-application
  ports:
    - name: python-application
      protocol: TCP
      port: 8080
