apiVersion: v1
kind: Service
metadata:
  name: app-python-server-service
spec:
  selector:
    app: app-python-server
  ports:
    - protocol: 'TCP'
      port: 8000
      targetPort: 8000
  type: LoadBalancer