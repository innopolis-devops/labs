apiVersion: v1
kind: Service
metadata:
  name: {{ include "chart-clocker.fullname" . }}
  labels:
    {{- include "chart-clocker.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "chart-clocker.selectorLabels" . | nindent 4 }}
