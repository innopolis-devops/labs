apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "chart-clocker.fullname" . }}-test-connection"
  labels:
    {{- include "chart-clocker.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "chart-clocker.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
