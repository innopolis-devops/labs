apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "charts-app-kotlin.fullname" . }}-test-connection"
  labels:
    {{- include "charts-app-kotlin.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "charts-app-kotlin.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
