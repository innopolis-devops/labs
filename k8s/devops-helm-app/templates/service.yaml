apiVersion: v1
kind: Service
metadata:
  name: {{ include "devops-helm-app.fullname" . }}
  labels:
    {{- include "devops-helm-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "devops-helm-app.selectorLabels" . | nindent 4 }}
