apiVersion: v1
kind: Service
metadata:
  name: {{ include "devops-time-app.fullname" . }}
  labels:
    {{- include "devops-time-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "devops-time-app.selectorLabels" . | nindent 4 }}
