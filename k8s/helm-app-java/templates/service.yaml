apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-app-java.fullname" . }}
  labels:
    {{- include "helm-app-java.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm-app-java.selectorLabels" . | nindent 4 }}
