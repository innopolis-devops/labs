apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "helm-app-python.fullname" . }}-config
  labels:
    {{- include "helm-app-python.labels" . | nindent 4 }}
data:
  config.json: |-
    {{ .Files.Get "files/config.json" }}
