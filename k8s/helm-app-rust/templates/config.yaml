apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "helm-app-rust.fullname" . }}-config
  labels:
    {{- include "helm-app-rust.labels" . | nindent 4 }}
data:
  config.json: |-
    {{ .Files.Get "files/config.json" }}