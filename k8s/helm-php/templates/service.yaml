apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-php.fullname" . }}
  labels:
    {{- include "helm-php.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm-php.selectorLabels" . | nindent 4 }}
