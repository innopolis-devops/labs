apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helm-php.fullname" . }}-test-connection"
  labels:
    {{- include "helm-php.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helm-php.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
