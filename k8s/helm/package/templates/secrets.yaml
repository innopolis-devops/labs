apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.secrets_name }}
  labels:
    app: {{ .Values.lable }}
type: Opaque
data:
  username: {{ .Values.username | b64enc | quote }}
  password: {{ .Values.password | b64enc | quote }}