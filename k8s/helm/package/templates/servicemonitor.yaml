apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ include "package.fullname" . }}
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app: {{ include "package.fullname" . }}
  endpoints:
  - interval: 5s
    port: http