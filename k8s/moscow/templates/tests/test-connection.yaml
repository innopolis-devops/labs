apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moscow.fullname" . }}-test-connection"
  labels:
    {{- include "moscow.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "moscow.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
