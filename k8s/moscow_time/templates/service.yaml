apiVersion: v1
kind: Service
metadata:
  name: {{ include "moscow_time.fullname" . }}
  labels:
    {{- include "moscow_time.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "moscow_time.selectorLabels" . | nindent 4 }}
