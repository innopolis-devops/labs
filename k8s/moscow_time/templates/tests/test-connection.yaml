apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moscow_time.fullname" . }}-test-connection"
  labels:
    {{- include "moscow_time.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "moscow_time.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
