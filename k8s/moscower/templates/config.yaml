apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "moscower.fullname" . }}-config
  labels:
    {{- include "moscower.labels" . | nindent 4 }}
data:
  config.json: |-
    {{ .Files.Get "files/config.json" }}
