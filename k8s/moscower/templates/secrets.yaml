apiVersion: v1
kind: Secret
metadata:
  name: {{ include "moscower.fullname" . }}-credentials
  labels: {{- include "moscower.labels" . | nindent 4 }}
type: Opaque
data:
  password: {{ .Values.password | b64enc | quote }}
