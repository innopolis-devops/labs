apiVersion: v1
kind: Service
metadata:
  name: {{ include "moscower.fullname" . }}
  labels:
    {{- include "moscower.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "moscower.selectorLabels" . | nindent 4 }}
