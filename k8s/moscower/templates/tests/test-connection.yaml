apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moscower.fullname" . }}-test-connection"
  labels:
    {{- include "moscower.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "moscower.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
