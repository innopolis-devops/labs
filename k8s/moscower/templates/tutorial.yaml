apiVersion: v1
kind: Pod
metadata:
  name: init-moscower
spec:
  containers:
  - name: nginx
    image: nginx
    ports:
    - containerPort: 80
    volumeMounts:
    - name: workdir
      mountPath: /store
  # These containers are run during pod initialization
  initContainers:
  - name: install
    image: busybox:1.28
    command:
    - wget
    - "-O"
    - "/store/index.html"
    - http://info.cern.ch
    volumeMounts:
    - name: workdir
      mountPath: "/store"
  dnsPolicy: Default
  volumes:
  - name: workdir
    emptyDir: {}
