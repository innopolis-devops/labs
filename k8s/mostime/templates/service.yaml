apiVersion: v1
kind: Service
metadata:
  name: {{ include "mostime.fullname" . }}
  labels:
    {{- include "mostime.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mostime.selectorLabels" . | nindent 4 }}
