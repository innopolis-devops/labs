apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mostime.fullname" . }}-test-connection"
  labels:
    {{- include "mostime.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mostime.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
