apiVersion: v1
kind: Service
metadata:
  name: exposer
  labels:
    app: moscow-time
spec:
  selector:
    app: moscow-time
  ports:
    - protocol: TCP
      port: 8080
  type: LoadBalancer