apiVersion: v1
kind: Service
metadata:
  name: moscow-time-web-app
spec:
  selector:
    app: moscow-time-web-app
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
  type: LoadBalancer