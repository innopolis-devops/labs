apiVersion: v1
kind: Service
metadata:
  name: exposer
  labels:
    app: py_app
spec:
  selector:
    app: py_app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: LoadBalancer
