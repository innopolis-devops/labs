apiVersion: v1
kind: Service
metadata:
  name: moscow
  labels:
    app: moscow-time
spec:
  selector:
    app: moscow-time
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: LoadBalancer