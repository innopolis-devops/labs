apiVersion: v1
kind: Service
metadata:
  name: pink-time
  labels:
    app: pink-time
spec:
  selector:
    app: pink-time
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
  type: LoadBalancer