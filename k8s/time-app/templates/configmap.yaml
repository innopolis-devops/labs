apiVersion: v1
kind: ConfigMap
metadata:
  name: time-app-configmap
  labels:
    {{- include "time-app.labels" . | nindent 4 }}
data:
  config.json: |-
    {{ .Files.Get "files/config.json" | nindent 4 }}