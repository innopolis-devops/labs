apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "time-application.fullname" . }}-test-connection"
  labels:
    {{- include "time-application.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "time-application.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
