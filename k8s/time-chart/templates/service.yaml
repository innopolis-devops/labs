apiVersion: v1
kind: Service
metadata:
  name: {{ include "time-chart.fullname" . }}
  labels:
    {{- include "time-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "time-chart.selectorLabels" . | nindent 4 }}
