apiVersion: v1
kind: Service
metadata:
  name: {{ include "time_app.fullname" . }}
  labels:
    {{- include "time_app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "time_app.selectorLabels" . | nindent 4 }}
