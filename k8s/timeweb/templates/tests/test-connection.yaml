apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "timeweb.fullname" . }}-test-connection"
  labels:
    {{- include "timeweb.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "timeweb.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
