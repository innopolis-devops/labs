apiVersion: v1
kind: Service
metadata:
  name: {{ include "new-helm-app.fullname" . }}
  labels:
    {{- include "new-helm-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "new-helm-app.selectorLabels" . | nindent 4 }}
