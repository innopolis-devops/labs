apiVersion: v1
kind: Service
metadata:
  name: {{ include "new-help-app.fullname" . }}
  labels:
    {{- include "new-help-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "new-help-app.selectorLabels" . | nindent 4 }}
