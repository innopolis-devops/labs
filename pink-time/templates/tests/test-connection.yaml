apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pink-time.fullname" . }}-test-connection"
  labels:
    {{- include "pink-time.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pink-time.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
