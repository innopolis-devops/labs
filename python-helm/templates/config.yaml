apiVersion: v1
kind: ConfigMap
metadata:
  name: python-config
  labels:
    {{- include "python-helm.labels" . | nindent 4 }}
data:
  config.json: |-
    {{ .Files.Get "files/config.json" | nindent 4 }}